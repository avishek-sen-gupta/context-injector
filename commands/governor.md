Control the Governor workflow enforcer. Subcommands:

- `/governor tdd` — activate the TDD state machine
- `/governor off` — deactivate
- `/governor status` — show current phase, blocked tools, and available transitions
- `/governor transition <target> [evidence_key]` — request a state transition with evidence
- `/governor evidence` — list captured evidence entries

The argument is: $ARGUMENTS
